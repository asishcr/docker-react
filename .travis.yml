language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t asishcr/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true asishcr/docker-react npm run test
